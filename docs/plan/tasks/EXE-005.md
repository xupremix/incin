# EXE-005 — capability registry and generated conformance matrix

1. [x] Audit the proposal contract, current dtype policy, operation taxonomy,
   backend feature boundaries, and unsupported-operation defaults.
2. [x] Add backend-neutral `CapabilityQuery`, `SupportLevel`,
   `UnsupportedReason`, `Capabilities`, and registry rule types under
   `incin_core::exec::capability`.
3. [x] Promote `KernelMathMode` to the core `MathMode` policy without leaving
   a parallel backend-local vocabulary.
4. [x] Define authoritative CPU, CUDA, and WGPU registrations covering dtype,
   layout, rank, training, and math-mode constraints.
5. [x] Make existing dtype-policy resolution consume the registry so advertised
   support and pre-execution rejection cannot drift.
6. [x] Generate `capability_matrix` cases from registrations and compare every
   advertised representative path with actual execution or a hardware-gated
   compile/runtime probe; assert stable unsupported reasons and no fallback.
7. [x] Run the exact row evidence, optional-backend checks, no-default/core,
   formatting, full workspace, diff-check, shape audit, and ledger validation.
8. [x] Record evidence and any justified deviations before marking complete.
