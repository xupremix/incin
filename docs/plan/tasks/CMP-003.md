# CMP-003 - Liveness and allocation planner with alias and peak-memory tests

**Theme:** compile  
**Tier:** preview  
**Deps:** CMP-002, PRF-001  
**Target:** `crates/incin-core/src/compiled/alloc.rs`  

Implements liveness analysis and buffer allocation planning for compiled graphs, with tensor aliasing and peak-memory tracking in `incin-core::compiled::alloc`.

## Steps

1. [ ] Create `crates/incin-core/src/compiled/alloc.rs`.
2. [ ] Export `pub mod alloc;` in `crates/incin-core/src/compiled/mod.rs` and re-export key types.
3. [ ] Implement `LivenessMap`, `AllocationPlanner`, and `MemoryPlan` with alias analysis and peak-memory tracking.
4. [ ] Create integration test suite `crates/incin-core/tests/compiled_alloc.rs`.
5. [ ] Run `cargo test -p incin-core --test compiled_alloc`.
