# CMP-001 - Capture the eager graph into validated IR with descriptor parity

**Theme:** compile  
**Tier:** preview  
**Deps:** EXE-009  
**Target:** `crates/incin-core/src/compiled/capture.rs`  

Implements eager graph capture into validated IR in `incin-core::compiled::capture`, validating graph structure, node dependencies, and operation descriptor parity.

## Steps

1. [x] Create `crates/incin-core/src/compiled/mod.rs` and `crates/incin-core/src/compiled/capture.rs`.
2. [x] Export `pub mod compiled;` in `crates/incin-core/src/lib.rs`.
3. [x] Implement `CapturedGraph` and `CapturedNode` with graph capture and topological validation.
4. [x] Create integration test suite `crates/incin-core/tests/compiled_capture.rs`.
5. [x] Run `cargo test -p incin-core --test compiled_capture`.
