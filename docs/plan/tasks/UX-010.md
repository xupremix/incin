# UX-010 — Typed einsum! with parser, shape, placement, and parity tests

**Theme:** ux  
**Tier:** exploratory  
**Deps:** EXE-003, DST-003  
**Target:** `crates/incin-macros/src/einsum.rs`  

Implements compile-time validated `einsum!` macro with subscript parser, input/output validation, and integration tests in `incin-macros`.

## Steps

1. [x] Record justification in `docs/plan/tasks/UX-010-justification.md`.
2. [x] Create `crates/incin-macros/src/einsum.rs`.
3. [x] Register `mod einsum;` and `pub fn einsum` in `crates/incin-macros/src/lib.rs`.
4. [x] Re-export `einsum` in `crates/incin/src/lib.rs` prelude.
5. [x] Create integration test suite `crates/incin-macros/tests/einsum_macro.rs`.
