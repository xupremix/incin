# GOV-004 - reproducible baseline suite

1. [x] Add a Criterion harness under `crates/incin/benches/` covering CPU
   capability probes and representative eager creation, elementwise, reduction,
   and matrix-multiplication workloads.
2. [x] Make optional GPU availability explicit in the capability baseline;
   unavailable hardware is recorded, never presented as a passing benchmark.
3. [x] Capture build time and release artifact sizes alongside runtime results.
4. [x] Record OS, architecture, CPU, memory, Rust/Cargo versions, enabled
   features, and GPU/toolchain availability in `docs/plan/baselines/` so results
   are reproducible and comparable.
5. [x] Run `cargo bench -p incin -- --save-baseline main` plus formatting,
   Clippy, tests, and ledger validation; record concrete output before marking
   complete.

GOV-004 complete. The required CPU command recorded eight Criterion series;
the optional WGPU command recorded three series on the Radeon 680M. CUDA is
explicitly unavailable rather than represented by a synthetic result.
`cargo test --workspace` completed with no failures, including 32 semantic
trybuild cases. Clippy was unavailable because the installed Rust 1.92
toolchain reports that `cargo-clippy` is not applicable.

