# GOV-005 — regression budgets and feature inventory

1. [x] Declare deterministic upper bounds for every runtime and compile-size
   series captured by GOV-004, with an explicit policy for adding or removing
   series.
2. [x] Inventory the public features and defaults of every feature-bearing
   workspace crate, including exact dependency forwarding and deprecated
   aliases.
3. [x] Add `cargo xtask budgets` to validate the baseline schema, complete and
   unique budget coverage, numeric bounds, feature defaults, and feature
   forwarding against the Cargo manifests.
4. [x] Add unit and fault-injection tests proving the gate rejects an exceeded
   budget, missing/duplicate series, and feature drift.
5. [x] Run the budget command in CI alongside the ledger and shape audits so
   every pull request enforces the declared repository contracts.
6. [x] Run formatting, xtask tests, the full workspace suite, diff-check, and
   ledger validation; record concrete evidence before marking complete.

Runtime, memory, cache, and accelerator-host performance execution remain the
