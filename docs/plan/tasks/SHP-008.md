# SHP-008 — witnessed tensor construction

1. [x] Replace the raw crate-wide `from_parts_unchecked` escape hatch with a
   constructor that can only consume a construction witness.
2. [x] Provide narrowly scoped witnesses for validated operation descriptors
   and storage-only transformations whose shape, dtype, and device metadata
   remain unchanged.
3. [x] Audit every current unchecked construction obligation (96 references at
   task start, including the constructor and checked-path delegation); route
   non-witnessed imports through `try_from_storage`.
4. [x] Add `construction_witness` coverage for accepted witnesses, rejected
   storage metadata, constructor privacy, and actionable `Flatten` diagnostics.
5. [x] Run the row evidence plus formatting, Clippy, core/workspace tests,
   no-default-features, shape audit, and ledger validation; record concrete
   evidence before marking the task complete.
