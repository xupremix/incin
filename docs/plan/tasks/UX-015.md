# UX-015 - PrecisionPolicy and LossScaling in incin-core::exec::precision

**Theme:** ux  
**Tier:** preview  
**Deps:** EXE-005, GRD-001  
**Target:** `crates/incin-core/src/exec/precision.rs`  

Implements `PrecisionPolicy` and `LossScaling` in `incin-core::exec::precision`, extending precision vocabulary with static/dynamic loss scaling and mixed precision configuration.

## Steps

1. [ ] Create `crates/incin-core/src/exec/precision.rs` with `LossScaling` and `PrecisionPolicy`.
2. [ ] Export `pub mod precision;` in `crates/incin-core/src/exec/mod.rs` and re-export `PrecisionPolicy`, `LossScaling` in `exec` and `prelude`.
3. [ ] Implement constructors, dynamic scaling updates, and conversions to/from `DTypeId`.
4. [ ] Create unit and parity test suite `crates/incin-core/tests/precision_policy.rs`.
5. [ ] Run `cargo test -p incin-core --test precision_policy`.
