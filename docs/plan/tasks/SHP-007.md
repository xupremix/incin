# SHP-007 - Mixed, named, and rank coverage

1. [x] Replace whole-shape broadcast matching with per-axis rules, including
   named, runtime, same-rank, and right-aligned operands.
2. [x] Add deterministic broadcast property tests and repair stale
   compile-fail fixtures.
3. [x] Relate mixed matmul contraction-axis types and check named/runtime
   contraction and batch values.
4. [x] Close the remaining unchecked rank-4 mixed matmul paths.
5. [x] Generate typed batched-matmul rules through `MAX_RANK` and prove every
   supported rank in the compile-pass suite; single-source the constructor
   argument ladder exposed by the rank-8 fixture.
6. [x] Add compile-fail coverage for an invalid high-rank contraction and keep
   diagnostics pinned to their semantic reason.
7. [x] Run `cargo test -p incin-core`, workspace fmt/test, the shape audit,
   no-default-features, and ledger validation; record evidence and mark
   SHP-007 complete. Clippy was unavailable in the installed Rust 1.92
   toolchain (`cargo-clippy` reported that its component was not applicable).
