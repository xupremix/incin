# CI-008 - Distributed macro trybuild suite for mesh!, placement!, #[parallel], and #[shard]

**Theme:** ci  
**Tier:** preview  
**Deps:** UX-002, UX-003, UX-004  
**Target:** `crates/incin-macros/tests/`  

Consolidates and validates the distributed macro trybuild test suite for `mesh!`, `placement!`, `#[parallel]`, and `#[shard]` under the `distributed` feature flag.

## Steps

1. [x] Add `distributed = ["incin/distributed"]` feature to `crates/incin-macros/Cargo.toml`.
2. [x] Create `crates/incin-macros/tests/distributed_macro_suite.rs` testing `mesh!`, `placement!`, `#[parallel]`, and `#[shard]` with feature `distributed`.
3. [x] Run `cargo test -p incin-macros --features distributed`.
