# UX-006 — `cargo incin tune` with an offline and stale-cache round trip

`UX-006` adds the `cargo incin tune` CLI subcommand to inspect, clear, and report on the autotuning cache (`AtomicTuningCache`).

1. [x] Create `incin::tune_report` module for inspecting and managing the autotuning cache directory.
2. [x] Implement CLI option handling for `--json`, `--clear`, and `--offline` flags.
3. [x] Add `subcommand == "tune"` to `cargo-incin.rs`.
4. [x] Implement integration tests in `crates/incin/tests/tune_cli.rs` covering text/JSON rendering, offline mode, and clear operation.

## Evidence

```text
cargo test -p incin --test tune_cli
4 passed; 0 failed
```
