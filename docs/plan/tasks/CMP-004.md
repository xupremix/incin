# CMP-004 - Constant folding, weight prepacking, and bounded shape buckets

**Theme:** compile  
**Tier:** preview  
**Deps:** CMP-002  
**Target:** `crates/incin-core/src/compiled/fold.rs`  

Implements constant folding, weight prepacking, and shape bucketing passes in `incin-core::compiled::fold`.

## Steps

1. [x] Create `crates/incin-core/src/compiled/fold.rs`.
2. [x] Export `pub mod fold;` in `crates/incin-core/src/compiled/mod.rs` and re-export key types in prelude.
3. [x] Implement `ConstantFolder`, `WeightPrepacker`, and `ShapeBucket` transformations.
4. [x] Create integration test suite `crates/incin-core/tests/compiled_fold.rs`.
5. [x] Run `cargo test -p incin-core --test compiled_fold`.
