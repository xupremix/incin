# UX-007 — Launcher and `#[distributed_main]` with shutdown and error tests

`UX-007` adds the `#[distributed_main]` attribute proc-macro in `incin-macros::distributed_main` for wrapping distributed entrypoints with CLI argument parsing (rank, world size) and panic/shutdown handling.

1. [x] Implement `distributed_main` attribute macro in `crates/incin-macros/src/distributed_main.rs`.
2. [x] Re-export `#[distributed_main]` in `crates/incin-macros/src/lib.rs`.
3. [x] Implement integration tests in `crates/incin-macros/tests/distributed_main.rs`.

## Evidence

```text
cargo test -p incin-macros --test distributed_main
1 passed; 0 failed
```
