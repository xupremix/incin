# CMP-002 - Immutable compiled plans and dynamic guards

**Theme:** compile  
**Tier:** preview  
**Deps:** CMP-001  
**Target:** `crates/incin-core/src/compiled/plan.rs`  

Implements `CompileOptions`, `FusionPolicy`, `DynamicShapePolicy`, `CompiledPlan`, and dynamic runtime shape/dtype guards in `incin-core::compiled::plan`.

## Steps

1. [ ] Create `crates/incin-core/src/compiled/plan.rs`.
2. [ ] Re-export `CompileOptions`, `FusionPolicy`, `DynamicShapePolicy`, `CompiledPlan`, `ShapeGuard` in `crates/incin-core/src/compiled/mod.rs` and `prelude`.
3. [ ] Implement immutable `CompiledPlan` wrapping `CapturedGraph` with guard verification and execution options.
4. [ ] Create integration test suite `crates/incin-core/tests/compiled_guards.rs`.
5. [ ] Run `cargo test -p incin-core --test compiled_guards`.
