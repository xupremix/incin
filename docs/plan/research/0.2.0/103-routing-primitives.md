#103 Routing primitives in catalog — M (scatter_add S; nonzero L blocked on #102)
Finding: none of one_hot sort nonzero repeat_interleave bincount scatter_add log_softmax logsumexp exist in the 174-op catalog. scatter overwrites (DuplicateIndexRule::Reject, last-write-wins) -> combining top-k outputs silently drops contributions. Profiles: Indexing=(TypedContract, Indexing output, Defined gradient); Scatter overrides Gradient::Undefined, deterministic=false.
Recommendation: land scatter_add FIRST (correctness bug): own op, Output::Preserve, GradientRule::Defined, deterministic=true, require DuplicateIndexRule::Accumulate (adjoint = scatter-add of grad through indices). Then log_softmax/logsumexp (S each), one_hot/bincount/repeat_interleave/sort (S-M). nonzero = forward-only, OutputRule::DataDependent, waits on #102. Sorted-segment vs atomic scatter_add must agree cross-backend (#83). Regenerate generated docs.
