#8 Public executor declaration macro — M
Finding: built-ins use private callback macro assert_every_advertised_row_executes! (replays the ONE capability declaration, emits const fn executes<O,B: Execute<O>>() per row -> advertise-without-execute fails to build) + per-result-shape shell macros (allocating/readback/pointwise/scalar/cmp/reduce) across cpu/metal/cuda/wgpu/candle. All private; fixture writes impls manually.
Recommendation: ship execute_impls! as macro_rules from incin-core::backend_authoring (zero parsing surface; seq!/best_device! precedent): entries (Op, handler[, out-type]), generic head impl<D: Device> MyBackend<D>, absolute ::incin paths (extend CI-005 decoys), emit shells + const coverage obligation from ONE entry list; custom $op:path supported (no built-in enum needed). Defer external_backend! umbrella (candle/ndarray/burn one-block bindings) until a second real consumer validates syntax. Proc-macro only if named diagnostics prove necessary.
Risk: hygiene regression on relative paths; downstream lacks shared declaration (author-supplied); premature syntax freeze.
