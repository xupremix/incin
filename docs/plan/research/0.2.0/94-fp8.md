#94 FP8 e4m3/e5m2 — L
Finding: scalar dtypes (StorageEncoding::scalar(1,1), DTypeKind::Float); impl_plain_builtin_dtype! seals half-crate types (f16/bf16 precedent); cudarc already maps __nv_fp8_e4m3/e5m2; DTypeKey serde table needs new arms; hub import hard-bounds f32 (needs widening or dequantize-on-load). No fp8 anywhere in cuda/.
Recommendation: TWO ConstDType newtypes via impl_plain_builtin_dtype! (forward e4m3 vs gradient e5m2 stays static, not dynamic); scale as separate f32 tensor REQUIRED at construction (to_f8_scaled) — silent-wrong-results otherwise; f32 accumulation recorded; capability rows Unsupported pre-Hopper; gate on #90/#85; admit in #93 serialization tables; seal decision from #96.
Risk: sealed-trait edit = core change; cuBLASLt K%16 constraints unverified.
