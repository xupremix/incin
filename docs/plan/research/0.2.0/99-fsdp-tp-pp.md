#99 FSDP / TP / PP execution — L (FSDP tier M given #97 transports)
Finding: plans PROVE everything: FsdpPlan ZeRO sharding + memory parity; TP=2 with ColumnOutputGather/RowOutputSum/HeadGather + divisibility; PP GPipe clocks + SendRecv boundaries; HybridPlanner -> AgreedPlan preflight. Backends (#97) implement AllReduce/AllGather/ReduceScatter/SendRecv in reference + NCCL. Missing: plan->execution lowering, partitioned matmul (#85/#90), Trainer accepting dist plans.
Recommendation: tiered — FSDP first on Data<N> meshes via reference transport (all-gather before use, free after; ReduceScatter grads NOT all-reduce), ZeRO-2 before ZeRO-3; TP second (column=Sharded weights, row=Partial<Sum> — placements already type-level); PP last (consume clocks as schedule). Add Trainer path consuming FsdpPlan+AgreedPlan rather than re-deriving.
Risk: reduce-scatter-vs-all-reduce confusion = correct-looking N× memory (verify by measurement); recomputation against freed params corrupts grads.
