#![allow(unused_imports)]
use incin::prelude::*;
use incin_data::{DataLoader, Dataset}; // Assumed standard paths, although I might need to check if they exist

#[test]
/// Test dataset.
fn test_dataset() {
    // Compile-only check that incin_data's Dataset/DataLoader types are
    // reachable through these import paths.
}
