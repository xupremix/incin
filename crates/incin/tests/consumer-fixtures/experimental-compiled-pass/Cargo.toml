[package]
name = "incin-facade-experimental-compiled-pass"
version = "0.0.0"
edition = "2024"

[workspace]

[dependencies]
incin = { path = "../../..", default-features = false, features = ["std", "compiled"] }
