error[E0277]: the trait bound `Forged: dim::sealed::Dim` is not satisfied
  --> tests/compile_fail/dim_trait_is_sealed.rs:10:5
   |
10 |     fn resolve_arg(_: Self::Arg) -> Result<usize, ShapeError> { Ok(1) }
   |     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ unsatisfied trait bound
   |
help: the trait `dim::sealed::Dim` is not implemented for `Forged`
  --> tests/compile_fail/dim_trait_is_sealed.rs:4:1
   |
 4 | struct Forged;
   | ^^^^^^^^^^^^^
   = note: `Forged` implements similarly named trait `Dim`, but not `dim::sealed::Dim`
   = help: the following other types implement trait `dim::sealed::Dim`:
             AddDim<A, B>
             BroadcastExtent<L, R>
             CheckedSubDim<A, B>
             ConstDim<N>
             ExactDivDim<A, B>
             MulDim<A, B>
             NamedDim<Tag, Extent>
             UInt<U, B>
           and $N others
note: required by a bound in `Dim`
  --> src/shapes/dim.rs
   |
   | pub trait Dim:
   |           --- required by a bound in this trait
   |     sealed::Dim
   |     ^^^^^^^^^^^ required by this bound in `Dim`
   = note: `Dim` is a "sealed trait", because to implement it you also need to implement `incin_core::shapes::dim::sealed::Dim`, which is not accessible; this is usually done to force you to use one of the provided types that already implement it
   = help: the following types implement the trait:
             incin_core::shapes::BroadcastExtent<L, R>
             usize
             incin_core::shapes::ConstDim<N>
             incin_core::shapes::NamedDim<Tag, Extent>
             incin_core::shapes::AddDim<A, B>
             incin_core::shapes::CheckedSubDim<A, B>
             incin_core::shapes::ExactDivDim<A, B>
             incin_core::shapes::MulDim<A, B>
           and $N others

error[E0277]: the trait bound `Forged: dim::sealed::Dim` is not satisfied
 --> tests/compile_fail/dim_trait_is_sealed.rs:6:14
  |
6 | impl Dim for Forged {
  |              ^^^^^^ unsatisfied trait bound
  |
help: the trait `dim::sealed::Dim` is not implemented for `Forged`
 --> tests/compile_fail/dim_trait_is_sealed.rs:4:1
  |
4 | struct Forged;
  | ^^^^^^^^^^^^^
  = note: `Forged` implements similarly named trait `Dim`, but not `dim::sealed::Dim`
  = help: the following other types implement trait `dim::sealed::Dim`:
            AddDim<A, B>
            BroadcastExtent<L, R>
            CheckedSubDim<A, B>
            ConstDim<N>
            ExactDivDim<A, B>
            MulDim<A, B>
            NamedDim<Tag, Extent>
            UInt<U, B>
          and $N others
note: required by a bound in `Dim`
 --> src/shapes/dim.rs
  |
  | pub trait Dim:
  |           --- required by a bound in this trait
  |     sealed::Dim
  |     ^^^^^^^^^^^ required by this bound in `Dim`
  = note: `Dim` is a "sealed trait", because to implement it you also need to implement `incin_core::shapes::dim::sealed::Dim`, which is not accessible; this is usually done to force you to use one of the provided types that already implement it
  = help: the following types implement the trait:
            incin_core::shapes::BroadcastExtent<L, R>
            usize
            incin_core::shapes::ConstDim<N>
            incin_core::shapes::NamedDim<Tag, Extent>
            incin_core::shapes::AddDim<A, B>
            incin_core::shapes::CheckedSubDim<A, B>
            incin_core::shapes::ExactDivDim<A, B>
            incin_core::shapes::MulDim<A, B>
          and $N others
