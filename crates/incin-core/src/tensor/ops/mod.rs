/// `binary`.
pub mod binary;
/// `index`.
pub mod index;
/// `loss`.
pub mod loss;
/// `manipulation`.
pub mod manipulation;
/// `module`.
pub mod module;
/// `reduce`.
pub mod reduce;
/// `unary`.
pub mod unary;

pub use index::{DTypeEq, IndexArgs, IndexSpec, ShapeEq};
