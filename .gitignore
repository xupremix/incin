target/
# The target-first allocation API is a source module at this path, not a
# build output directory; consumer-fixtures/* below are separate standalone
# crates whose own `target/` build dirs should stay ignored.
!crates/incin-backends/src/target/
!crates/incin-backends/src/target/**
scratch/
# mdBook build output for docs/book - generated by CI on every push, never committed.
docs/book/book/
docs/book/site/
docs/architecture/*.visual-check.*
__pycache__/
.DS_Store
**/*.rs.bk
*.swp
.idea/
.vscode/
*.onnx
!crates/incin/resnet18.onnx
!crates/incin/tests/fixtures/*.onnx
!test_models/*.onnx
*.safetensors
*.incin_meta
expanded.rs
data/
wip/
.venv/

DESIGN.md
FUTURE_ROADMAP.md
NATIVE_BACKEND_PLAN.md
graphify-out/
# cargo-fuzz run state (issue #48): corpus/artifacts regenerate locally and
# the scheduled CI job starts fresh; only fuzz/Cargo.toml, rust-toolchain.toml
# and fuzz_targets/ are sources.
fuzz/corpus/
fuzz/artifacts/
